const score = 400
// console.log(score);

const balance = new Number(100)
// console.log(balance);

// console.log(balance.toString().length);
// console.log(balance.toFixed(2));

const otherNum = 123.8966
// console.log(otherNum.toPrecision(4));

const hundreds = 1000000
// console.log(hundreds.toLocaleString()); // en-IN


// ************************* Maths ************************

// console.log(Math); // take a look for the functions in console
// console.log(Math.abs(-4)); // only if there are -ve values, it will change to positive. +ve will be positive only.
// console.log(Math.round(4.6));
// console.log(Math.ceil(4.2));  // ceil means Top...means if the number is little greater than 4 then it prints greater number than 4
// console.log(Math.floor(4.2));
// console.log(Math.min(1,2,3,4));
// console.log(Math.max(1,2,3,4));
// sqrt, pow, trance

console.log(Math.random());   // it gives you the values between 0 and 1
console.log((Math.random() * 10) + 1);  // why + 1 because 0 will not be printed
console.log(Math.floor(Math.random() * 10) + 1);  // small value

const min = 10
const max = 20
console.log(Math.floor(Math.random() * (max - min + 1)) + min);  // important line















